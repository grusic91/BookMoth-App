import { GET_ALL_BOOKS } from "../actionTypes";

const initialState = {
  books: []
}

export default (state = initialState, action) {
  switch (action.type) {
    case GET_ALL_BOOKS:
    

      break;
    default:

  }
}
